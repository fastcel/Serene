<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_gradient_drawer"
    android:padding="24dp">


    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/create_your_avatar"
        android:textColor="@color/text_color_purple"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginTop="25dp"/>

    <TextView
        android:id="@+id/tvSubtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/use_the_arrows_to_customise"
        android:textColor="@color/text_color_purple"
        android:textSize="13sp"
        android:layout_marginTop="2dp"/>


    <!-- CENTER AREA -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Avatar -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardAvatarPreview"
            android:layout_width="180dp"
            android:layout_height="180dp"
            app:cardCornerRadius="24dp"
            app:cardElevation="0dp">

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/light_blue">

                <com.example.serene.AvatarView
                    android:id="@+id/avatarView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"/>
            </FrameLayout>
        </androidx.cardview.widget.CardView>

        <!-- Controls -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:orientation="vertical"
            android:backgroundTint="@color/light_blue"
            android:background="@drawable/bg_controls_rounded"
            android:padding="12dp">

            <!-- Accessory -->
            <include
                android:id="@+id/rowAccessory"
                layout="@layout/item_avatar_control"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/purple"
                android:layout_marginHorizontal="8dp"/>

            <!-- Face -->
            <include
                android:id="@+id/rowFace"
                layout="@layout/item_avatar_control"/>

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/purple"
                android:layout_marginHorizontal="8dp"/>

            <!-- Color -->
            <include
                android:id="@+id/rowColor"
                layout="@layout/item_avatar_control"/>

        </LinearLayout>

    </LinearLayout>


    <!-- BUTTONS -->
    <Button
        android:id="@+id/btnLetsGo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="15dp"
        android:text="@string/let_s_go"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:backgroundTint="@color/purple"
        android:stateListAnimator="@null"/>

    <Button
        android:id="@+id/btnSkip"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="@string/skip_for_now"
        android:textColor="@color/text_color_purple"
        android:textSize="14sp"
        android:background="@android:color/transparent"
        android:layout_marginTop="6dp"/>

</LinearLayout>